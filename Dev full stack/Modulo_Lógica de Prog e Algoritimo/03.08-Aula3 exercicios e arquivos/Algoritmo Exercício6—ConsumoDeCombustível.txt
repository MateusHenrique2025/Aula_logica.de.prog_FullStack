Algoritmo "Exercício6—ConsumoDeCombustível"

// Calcule o consumo de combustível de um carro (consumo = distância / litros).
Var
// Seção de Declarações das variáveis 
	distancia, litros, consumototal: real
  
Inicio
// Seção de comandos, procedimentos, funções, operador
	//valor da distância percorrida
  escreval ("Digite a distancia(km): ")
	leia(distancia)
  //litros de gasolina 
  escreval ("Digite quantos litros de gasolina(L) : ")
	leia(litros)

  consumototal <- distancia / litros
	escreval ("consulmo de conbustivel do carro(km/L): ", consumototal)
  
fimalgoritmo